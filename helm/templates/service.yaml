apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: dev  
spec:
  selector:
    app: my-golang-deploy
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
  type: {{  .Values.service.type }}