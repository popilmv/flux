apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm27.fullname" . }}
  labels:
    {{- include "helm27.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm27.selectorLabels" . | nindent 4 }}
